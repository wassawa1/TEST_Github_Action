[pytest]
; `tests` ディレクトリ内のテストファイルを見つけて実行する
testpaths = tests 

; テストを詳細に表示するためのオプションを追加
; `-v` は詳細（verbose）モードを有効にし、`-v` を2回指定することでさらに詳細な情報を表示
; `-s` は標準出力をキャプチャしないオプション
; カバレッジ調査を有効化
addopts = -vvs --cov=. --cov-report=xml